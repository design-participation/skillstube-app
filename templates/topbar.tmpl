<nav class="navbar navbar-light bg-light">
	<a class="navbar-brand" href="/"> 
		{% if not nologo %} 
			{% include 'logo.tmpl' %} 
		{% else %}
			<span class="logo-text">How-To-App </span>
		{% endif %}</a>
	{% if user %}
		<ul class="nav">
			<li class="nav-item">
				<a class="nav-link" href="/search"><i class="fas fa-search"></i> <span class="d-none d-md-inline">Search</span></a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="/friends"><i class="fas fa-user-friends"></i> <span class="d-none d-md-inline">Friends</span></a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="/shared"><i class="fas fa-share-alt"></i> <span class="d-none d-md-inline">Shared</span></a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="/playlists"><i class="fas fa-star"></i> <span class="d-none d-md-inline">Playlists</span></a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="/logout"> <i class="fas fa-sign-out-alt"></i> <span class="d-none d-md-inline">Logout</span> </a>
			</li>
		</ul>
	<div class="user">
		<a href="/notifications"><img class="user-picture-sm" src="{{ user['picture'] }}">
			{% if user['notification_count'] > 0 %}
			<span class="notification-count badge badge-primary">{{ user['notification_count'] }}</span>
			{% endif %}
		</a>
	</div>
	{% else %}
	<div>
		<a class="nav-link" href="/login"> <i class="fas fa-sign-in-alt"></i> Login </a>
	</div>
	{% endif %}
	</div>
</nav>
<nav aria-label="breadcrumb">
	<ol class="breadcrumb bg-transparent">
		{% for item in breadcrumb %}
			{% if loop.last %}
			<li class="breadcrumb-item active" aria-current="page"><i class="fas fa-{{ item.icon }}"></i> {{ item.text }}</li>
			{% else %}
			<li class="breadcrumb-item"><a href="{{ item.link }}"><i class="fas fa-{{ item.icon }}"></i> {{ item.text }}</a></li>
			{% endif %}
		{% endfor %}
	</ol>
</nav>
